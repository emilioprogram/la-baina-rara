let lista: number[] = []
radio.onReceivedValue(function (name, value) {
    if (false) {
        let sprite: game.LedSprite = null
        pins.setAudioPinEnabled(led.point(lista.length, 2))
        sprite.delete()
        game.gameOver()
        lista = 0
    }
})
